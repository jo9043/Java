package section2;

/* 
 * 생성자
 * 
 * 클래스 안에 그 클래스와 동일한 이름을 가지며 return 타입이 없는 특별한 메서드
 * new 명령으로 객체가 생성될 때 자동으로 실행된다.
 * 객체의 데이터 필드의 값을 초기화 하는 것이 주 목적.
 */

public class Term3 {
	int coef;
	int expo;
	
	public int calcTerm(int x){
		return (int)(coef * Math.pow(x, expo));
	}
	
	public void printTerm(){
		System.out.println(coef + "x^" + expo);
	}
}

/*
 * Term2 t = new Term2();
 * t.coef = 3;
 * t.expo = 2;
 * int result = t.calcTerm(2);
 * t.printTerm();
 * 
 * Term2는 설계도 일 뿐.
 * 클래스가 아닌 각각의 객체 안에 있는 메서드들을 실행 하는 것. (클래스 안의 메서드들은 허상)
 * 각각의 객체 안에 calcTerm과 printTerm 메서드가 존재.
 * 데이터멤버 뿐만 아니라 각각의 메서드들도 객체마다 따로 존재.
 */

/*
 * 객체란: 데이터 + 메서드
 * 데이터는 객체의 정적 속성을 표현, 메서드는 객체의 기능(동적 속성)을 표현
 * 
 */



