package Section2;

public class Term2 {
	int coef;
	int expo;
	
	int calcTerm(int x){
		return (int)(coef * Math.pow(x, expo));
	}
	
	void printTerm(){
		System.out.println(coef + "x^" + expo);
	}
}

/*
 * Term2 t = new Term2();
 * t.coef = 3;
 * t.expo = 2;
 * int result = t.calcTerm(2);
 * t.printTerm();
 * 
 * Term2는 설계도 일 뿐.
 * 우리가 실행하는 것은 클래스가 아니라 각각의 객체 안에 있는 메서드들.
 * 각각의 객체 안에 calcTerm과 printTerm 메서드가 존재.
 * 데이터멤버 뿐만 아니라 각각의 메서드들도 객체마다 따로 존재.
 */
